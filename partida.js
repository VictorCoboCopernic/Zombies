let partida = {
    medida_tabla = 5,
    medidaida_casillas_tabla = 25,
    tabla:[],
	/*Funciones*/
	
    inicializar_tabla:function()´{
		for(let i = 0 ; i < medida_tabla; i ++)
			tabla [i] = [];	
		for(let j = 0; j < array.length;j++){
			const element = array[j]
		}			
	}
	
    inciar:function (mida){
        this.mida_tauler= mida;
        this.mida_caselles_tauler = mida * mida;
    }
}